name: y2notes2
description: A cross-platform note-taking app with magical writing effects
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  go_router: ^12.0.0
  perfect_freehand: ^2.0.0
  uuid: ^4.2.1
  equatable: ^2.0.5
  vector_math: ^2.1.4
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
